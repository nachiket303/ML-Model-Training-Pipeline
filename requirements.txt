# Reproducibility lock: exact versions verified on Python 3.13.1 (target >=3.11).
# Direct dependencies plus the numerically-critical transitive libraries (numpy/scipy/joblib)
# that influence model reproducibility. Install with: pip install -r requirements.txt
#
# Runtime
pandas==2.3.3
numpy==2.5.0
scipy==1.18.0
scikit-learn==1.9.0
joblib==1.5.3
pyyaml==6.0.3
pydantic==2.13.4
typer==0.26.8
mlflow==3.14.0
pyarrow==24.0.0

# Dev / tooling
pytest==9.1.1
black==26.5.1
ruff==0.15.20
